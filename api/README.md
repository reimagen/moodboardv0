# API Placeholder

This folder is reserved for future backend/API routes (e.g., Next.js route handlers or server functions). Keep outbound provider callers under `clients/` and have API handlers call them rather than invoking providers directly from the UI.
